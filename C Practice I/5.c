//prints the area and circumference for a circle with radius r
void eval_circle(int r);

//returns index of val in arr
//returns -1 if val is not in arr
int find(int val, const int* arr);

//prints the most frequent char in str
void max_freq_char( const char* str );

//perfoms matrix-matrix multiplication of A and B
//leaving the result in C
void matrix_mul( const int** A, const int** B, int** C);

int main(){
	return 0;
}